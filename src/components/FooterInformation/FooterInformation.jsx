


function FooterInformation({title, sectionText}) {

    return (
        <section>
            <h2>{title}</h2>
            <p>{sectionText}</p>
        </section>
    )
}

export default FooterInformation